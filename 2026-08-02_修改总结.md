# 2026-08-02 修改总结

## 一、建筑碰撞箱重构

### 1.1 取消工厂和海军节点碰撞箱
- 碰撞检测仅遍历 `CITIES` 数组，工厂和海军节点本来就不在碰撞逻辑中
- 明确规则：工厂和海军节点不设置碰撞箱、不设置点击检测圈

### 1.2 统一选中圈 = 碰撞箱 = 点击检测圈
- 之前三个圈各自定义，互不一致
- 现在写死为同一公式，四文件同步：

| 城市类型 | 旧值 | 新值 |
|---------|------|------|
| 首都 | 3.0 → 2.0 → **2.5** | 大幅缩小 |
| 大城市 | 5.5 → **4.5** | 小幅缩减 |
| 小城市 | 3.0 → 2.0 → **2.5** | 小幅增大 |

### 1.3 修改文件
| 文件 | 修改内容 |
|------|---------|
| `js/game_ui.js` | 选中圈渲染：selR 值更新 |
| `js/game_core.js` | 单位碰撞分离：selR 值更新 |
| `js/ai/ai_pathfinding.js` | AI 寻路碰撞：selR 值更新 + 分母从 bEffZoom 改为 zoom |
| `js/game_state.js` | 点击检测 findCityAtScreen：从固定 25px 改为 selR 公式 |

### 1.4 修复的一致性 bug
- `ai_pathfinding.js` 中碰撞半径转换用了 `bR = selR / (bEffZoom * PPD)`，而 `game_core.js` 用 `selR / (zoom * PPD)`，导致缩放低于战术缩放时碰撞箱不一致
- 已统一为 `selR / (zoom * PPD)`

## 二、首都大小减半

- `game_ui.js`：`bImgSize = 10 * bEffZoom * 3` → `5 * bEffZoom * 3`（30 → 15）

## 三、粮食存储上限调整

- `config.js`：
  - 小城市 `grainMax`：150 → 350
  - 大城市 `grainMax`：400 → 1000

## 四、文档

- 新增 `建筑选中圈规则.md`：记录选中圈/碰撞箱/点击圈统一规则及四文件代码位置
- 新增 `2026-08-02_修改总结.md`：本日修改总结
- 更新 `project_memory.md`：同步新规则值