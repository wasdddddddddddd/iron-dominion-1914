# Iron & Dominion 1914 — 单位与建筑属性模板

> 本文档是新增兵种/舰船/建筑时的**属性模板**，所有数值与代码位置均以当前版本为准。
> 三个基准参照物：陆军＝步兵、海军＝战列舰（高品级舰）、建筑＝首都/大城市。

---

## 一、陆军模板（基准：步兵）

### 1. 字段定义（`js/game_state.js` → `UNIT_TYPES`）

```js
infantry: {
  cost: 50,        // 造价（$）
  manpower: 15,    // 消耗人力
  range: 0.204,    // 射程（经度°，以步兵=1 换算显示）
  fireRate: 1,     // 射速（天/发）
  damage: 14,      // 单发伤害
  speed: 0.0432,   // 基准移速（°/天，移动循环再 ×2.5）
  maxStr: 100,     // 最大兵力（HP）
  bulletSpeed: 1.4,// 子弹速度（视觉/命中）
  sym: "🪖",       // 图标 emoji
  img: "images/unit_infantry.png", // 通用贴图
  label: "步兵",   // 面板显示名
  desc: "基准，全能主力"
}
```

### 2. 陆军数值表

| 兵种 | 造价 | 人力 | 伤害 | 射速(天/发) | 射程(° / 倍率) | 移速(° / 倍率) | 兵力 | 贴图 | 图标 |
|------|------|------|------|------|------|------|------|------|------|
| 步兵 | 50 | 15 | 14 | 1.0 | 0.204 / **1.0** | 0.0432 / **1.0** | 100 | unit_infantry.png | 🪖 |
| 山地师团 | 65 | 12 | 12 | 1.0 | 0.204 / 1.0 | 0.0432 / 1.0 | 95 | unit_mountain_new.png | ⛰️ |
| 工兵 | 70 | 12 | 8.4 | 1.25 | 0.1428 / 0.7 | 0.0389 / 0.9 | 110 | unit_engineer.png | 🛠️ |
| 骑兵 | 80 | 10 | 11.2 | 1.43 | 0.1224 / 0.6 | 0.0648 / **1.5** | 90 | unit_cavalry.png | 🏇 |
| 炮兵 | 120 | 8 | 35 | 5 | 0.675 / **3.3** | 0.0259 / 0.6 | 70 | unit_artillery.png | 💥 |
| 空军 | 200 | 3 | 13 | 0.8 | 0.45 / 2.2 | 0.18 / **4.2** | 40 | units/generic/airplane.png | ✈️ |

### 2b. 空军模板（空中单位，`airplane`）

- **机动 70%**：固定闪避子弹率（`navyDodgeRate` 特判），面板显示"✈️ 机动: 70%（闪避子弹）"
- **空中豁免**（移动循环）：无视地形减速（山地/海岸）、无视外交国界、可飞越水域（不倒退、目标在水上不修正）
- **高度视觉**：贴图绘制抬高 `imgSize × 0.35`，脚底影子留在原地，产生飞行高度感
- 六列强专属贴图 `images/units/{国家}/airplane.png`，不像素化；工期 6 天；生产按钮硬编码造价需与 UNIT_TYPES 同步


### 3. 移速与地形修正

- 实际移速：`speed × days × 2.5`（一天上限 `ed = min(days, 0.04)`）
- 地形减速（`js/config.js` → `TERRAIN_MOVE` + `MOUNTAIN_MOVE_BY_TYPE`，逐格查询，仅陆军生效）：
  - 平原 1.0
  - 山地按兵种：步兵/工兵 0.55（-45%）、**山地师团 0.95（-5%）**、骑兵 0.40（-60%）、炮兵 0.50（-50%）
  - **空军无视一切地形减速**
- 海岸减速：贴近海岸 0.85 → 0.7 → 0.5（`gPF.cost` 分级）；空军无视
- 炮兵**在山地射程 +20%**（`ARTILLERY_MOUNTAIN_RANGE_BONUS`），射程圈同步放大

### 4. 绘制大小与选中圈（`js/game_panels.js`）

```js
BASE = 7; // 全局基准像素
imgSize = BASE * 1.75 * zoom;   // 陆军贴图边长 = 12.25·zoom
selR    = imgSize * 0.65;       // 选中圈半径
外交圈 = selR + 4;              // 选中时按敌/友/中立着色
```

- 选中圈随 zoom 等比缩放；命中判定约 ±6px
- 六列强（德奥俄英法意）使用国家专属贴图 `images/{国家}/{type}.png`（仅步兵/骑兵），其余国家与山地师团用通用贴图

---

## 二、海军模板（基准：战列舰）

### 1. 舰船六维（`js/navy/shipGrades.js` → `SHIP_GRADES`，乘数制）

| 品级 | 名称 | 速度 | 射程 | 射速 | 威力 | 生命 | 机动 |
|------|------|------|------|------|------|------|------|
| T1 | 朽坏 | -0.10 | -0.10 | -0.10 | -0.12 | -0.12 | +0.06 |
| T2 | 老旧 | -0.12 | +0.06 | -0.12 | -0.08 | -0.10 | -0.08 |
| T3 | 普通 | 0 | 0 | 0 | 0 | 0 | 0 |
| T4 | 精锐 | +0.08 | +0.05 | +0.10 | +0.06 | +0.08 | -0.04 |
| T5 | 新锐 | +0.16 | -0.04 | +0.16 | +0.04 | +0.05 | +0.12 |
| T6 | 旗舰 | +0.04 | +0.12 | -0.04 | +0.06 | +0.16 | -0.06 |
| T7 | 英雄级 | +0.10 | +0.14 | +0.08 | +0.16 | +0.12 | +0.10 |
| T8 | 传奇级 | 0 | 0 | 0 | 0 | 0 | 0 |

- T8 传奇级六维归零，由专属名舰覆盖（巴伐利亚号/国王号/无畏号/皇家橡树号/法兰西号，`T8_LEGENDARY_SHIPS`）
- 舰名池：`js/navy/shipData.js` → `SHIP_NAMES`（按国家 × 品级）

### 2. 舰船属性公式（`js/game_core.js` → `applyNavyShipStats`）

基准 = `UNIT_TYPES.navy`：造价 500、人力 5、射程 0.816、射速 1.5 天/发、伤害 80、速度 0.0675、兵力 500

```
navySpd = 0.0675 × (1 + ship.speed)
navyRng = 0.816  × (1 + ship.range)
navyFr  = 1.5    / (1 + ship.fireRate)      // 射速越低越快
navyDmg = 80     × (1 + ship.power)
maxHp   = 500    × (1 + ship.hp)
navyMvr = ship.maneuver                     // 闪避率 = 5% + navyMvr
```

### 3. 战列舰实例：无畏号（T8 传奇级专属，属性覆盖品级默认值：speed 0.14、range 0.24、fireRate 0.12、power 0.18、hp 0.16、maneuver 0.10）

| 属性 | 公式 | 结果 |
|------|------|------|
| 移速 | 0.0675 × 1.14 | 0.0770（≈步兵 1.78 倍） |
| 射程 | 0.816 × 1.24 | 1.0118（≈步兵 4.96 倍） |
| 射速 | 1.5 / 1.12 | 1.34 天/发 |
| 伤害 | 80 × 1.18 | 94.4 |
| 生命 | 500 × 1.16 | 580 |
| 机动/闪避 | 5% + 0.10 | 15% |

### 4. 绘制大小与选中圈（`js/game_panels.js`）

```js
imgSize = BASE * 5 * zoom;    // 海军贴图边长 = 35·zoom（陆军约 2.86 倍）
selR    = imgSize * 0.55;     // 选中圈半径
脚底阴影 偏下 0.15 倍 imgSize；透明度 0.92（陆军 0.85）
```

- 实际移速：`navySpd × days × 2.5`；海军不受地形减速影响
- 一字阵：阵型间距 `spacing = 0.12`（°），formationIndex 0,±2,±4… 偏移 `(idx - 锚点) × spacing / 2`

---

## 三、建筑模板（基准：首都/大城市）

### 1. 城市三级（`js/game_state.js` 初始化 → `G.cities`）

| 类型 | 生命 maxHp | 攻击伤害 | 攻击射程(°) | 射速 | 贴图边长 | 选中圈半径(hlSize×0.55) | 填充圈(hlSize×0.7) |
|------|------|------|------|------|------|------|------|
| 首都 | **500** | 50 | 0.30 | 1 天/发 | `20·zoom·3` = 60·zoom | 59.4·zoom | 75.6·zoom |
| 大城市 | **200** | 50 | 0.30 | 1 天/发 | `10·zoom·2` = 20·zoom | 30.8·zoom | 39.2·zoom |
| 小城市 | **100** | 30 | 0.24 | 1 天/发 | `9·zoom·2` = 18·zoom | 18.2·zoom | 23.1·zoom |

- 城市攻击：红色子弹、溅射 0.02，只攻击可交战敌人（`js/game_core.js` 城市攻击段，vRange/vDamage/vFireCd=1）
- 攻击范围圈：选中城市时绘制（小城市 0.24 / 大与首都 0.30，`game_panels.js`）
- 贴图：`images/building_capital.png`（首都用国家专属 `{国家}_capital`）/ `building_major.png` / `building_small.png`
- 小城市可升级大城市（$150 / 40 天）→ maxHp 升为 200

### 2. 工厂（`G.factories`）

| 项 | 值 |
|------|------|
| 生命 | 30 / 30（不可攻击，仅被摧毁） |
| 绘制 | `18·zoom·2` = 36·zoom，贴图 `building_factory.png` |
| 血条 | 26×4 px（仅受损时显示） |
| 选中框 | 固定 14×14 px（双击，不随缩放） |
| 造价/工期 | $50 / 10 天 |

### 3. 海军节点（`G.navyNodes`，海军基地）

| 项 | 值 |
|------|------|
| 生命 | **3000** |
| 攻击 | 伤害 60、射程 `0.816×1.2 = 0.979`（≈步兵 4.8 倍）、射速 0.5 天/发（步兵 2 倍） |
| 治愈 | healRadius 0.15（回复范围内己方舰船） |
| 绘制 | `20·zoom·2` = 40·zoom，贴图 `building_naval.png` |
| 选中圈 | `nSize × 0.32`，HP 条在其下方 |
| 生产 | 生产舰船（品级按节点等级 roll），升级节点提升品级池 |

---

## 四、新增单位/建筑清单（速查）

新增**兵种**：
1. `game_state.js` UNIT_TYPES 加条目（含 img/label/manpower）
2. `config.js`：UNIT_FULL_NAME、UNIT_SHORT、必要的 TERRAIN_MOVE/加成常量
3. `game_panels.js`：单城生产 types + 样式 + 多城 typeDefs + majBtns
4. `game_core.js` + `ai_controller.js`：buildDays 映射（默认 3 天）
5. 特殊机制（地形减速/射程加成）：`ai_pathfinding.js` 6c 段 + 面板显示段
6. bump index.html 全部 `?v=` 版本号（node 脚本，勿用 PowerShell）

新增**舰船**：`shipGrades.js` 加品级或国家名舰 → `shipData.js` 加入名池 → 命名规则 `shipNaming.js`

新增**建筑**：`game_state.js` 初始化 + `BUILDING_IMAGES` 预加载 + 绘制/选中圈（game_ui.js / game_panels.js）+ 攻击逻辑（game_core.js 攻击段）
