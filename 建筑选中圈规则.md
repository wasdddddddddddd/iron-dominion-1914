# 建筑选中圈规则

## 核心原则

**建筑选中圈 = 碰撞箱 = 点击检测圈，三者完全统一，写死为同一公式，禁止分开定义。**

## 适用范围

仅对**城市（capital / major / small）**生效。工厂和海军节点**不设置**选中圈、碰撞箱、点击检测圈。

## 大小公式

所有圈的大小 = `selR * bEffZoom`，其中 `bEffZoom = max(zoom, TACTICAL_ZOOM)`。

| 城市类型 | selR 系数 | 说明 |
|---------|----------|------|
| 首都（capital） | 2.5 | 大幅缩小，便于单位靠近 |
| 大城市（major） | 4.5 | 适中，覆盖主要城市 |
| 小城市（small） | 2.5 | 小幅增大，便于点击 |

## 涉及文件与代码位置

### 1. 选中圈渲染 — `game_ui.js`

```javascript
// 选中光圈（单选或框选城市），选中圈=碰撞箱=点击圈
let selR = city.isCapital ? 2.5 * bEffZoom : (isMajor ? 4.5 * bEffZoom : 2.5 * bEffZoom);
ctx.beginPath(); ctx.arc(sx, sy - 10, selR, 0, Math.PI * 2);
```

### 2. 单位碰撞分离 — `game_core.js`

```javascript
// 选中圈=碰撞箱=点击圈：首都 2.5*effZoom，大城市 4.5*effZoom，小城市 2.5*effZoom
let selR = city.isCapital ? 2.5 * bEffZoom : (isMajor ? 4.5 * bEffZoom : 2.5 * bEffZoom);
let bR = selR / (zoom * PPD);  // 注意分母是 zoom，不是 bEffZoom
```

### 3. AI 寻路碰撞 — `js/ai/ai_pathfinding.js`

```javascript
// 选中圈=碰撞箱=点击圈：首都 2.5*effZoom，大城市 4.5*effZoom，小城市 2.5*effZoom
let selR = city.isCapital ? 2.5 * bEffZoom : (isMajor ? 4.5 * bEffZoom : 2.5 * bEffZoom);
let bR = selR / (zoom * PPD);  // 注意分母是 zoom，不是 bEffZoom
```

### 4. 点击检测 — `game_state.js` 的 `findCityAtScreen()`

```javascript
// 点击圈 = 选中圈 = 碰撞箱：首都 2.5*effZoom，大城市 4.5*effZoom，小城市 2.5*effZoom
let isMajor = majorCities.has(city.id) || isMajorCity(city.id);
let selR = city.isCapital ? 2.5 * bEffZoom : (isMajor ? 4.5 * bEffZoom : 2.5 * bEffZoom);
let dist = Math.hypot(sx - cx, sy - cy);
if (dist < selR && dist < bestDist) { best = city; bestDist = dist; }
```

## 关键注意事项

1. **碰撞转换时分母用 `zoom`**：碰撞半径从像素转为世界坐标时，`bR = selR / (zoom * PPD)`，分母是 `zoom` 而非 `bEffZoom`。这是之前一致性的 bug，已修复。
2. **三处公式必须同步修改**：修改选中圈大小时，必须同时更新 `game_ui.js`、`game_core.js`、`ai_pathfinding.js`、`game_state.js` 四个文件。
3. **工厂和海军节点不参与**：碰撞遍历只走 `CITIES` 数组，不包含工厂和海军节点。
4. **点击检测不再用固定 25px**：`findCityAtScreen` 之前用 `bestDist = 25` 固定像素判断，现改为 `selR` 公式，与碰撞箱/选中圈完全一致。